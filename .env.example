# Server port
PORT=3000

# Host path for downloads (mounted into container at /downloads)
DOWNLOAD_PATH=./downloads

# Default settings (used on first run, then stored in data/settings.json)
DEFAULT_FORMAT=mp3
DEFAULT_AUDIO_QUALITY=192
DEFAULT_VIDEO_QUALITY=best
DEFAULT_LANG=de
